# Name: Terence Anquandah
# Index Number: 10022200077

STUDENT_NAME=Terence Anquandah
INDEX_NUMBER=10022200077

# LLM provider (Vertex AI only)
LLM_PROVIDER=vertex

# Vertex AI
PROJECT_ID=intro-to-ai-494310
LOCATION=us-central1
GEMINI_MODEL=gemini-2.5-flash

# App
LOG_LEVEL=INFO

# --- Cloud deployment (Render) ---
# Paste the entire service account JSON as a single-line string value.
# Generate via: cat service-account.json | python -c "import json,sys; print(json.dumps(json.load(sys.stdin)))"
GOOGLE_APPLICATION_CREDENTIALS_JSON=

# Comma-separated list of allowed frontend origins (add your Render frontend URL)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.onrender.com
